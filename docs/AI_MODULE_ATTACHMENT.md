# AI Module Attachment

`memory/module_attachment.json` is the main AI-facing object.

It points to frame index, events, OCR index, digest, receipts, chunk map, bundle manifest, memory spine, and seed spine.
